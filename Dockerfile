#Готовый образ
FROM openjdk:8-slim
#Установка рабочего каталога внутри образа
WORKDIR /usr/src/myapp
#Копируем файлы из текущего каталога в файловую систему образа
COPY . /usr/src/myapp
#Запуск приложения (Команда запуска)
CMD ["java", "-jar", "db-api-for-docker.jar"]
#Порт 9999 будет слушать контейнер
EXPOSE 9999